apiVersion: v1
kind: Secret
metadata:
  name: {{ include "satisfactory-saas.fullname" . }}-secrets
  labels:
    {{- include "satisfactory-saas.labels" . | nindent 4 }}
type: Opaque
data:
  # ... (secretos existentes de DB y NextAuth)
  {{- if .Values.errorTracking.enabled }}
  SENTRY_DSN: {{ .Values.errorTracking.dsn | b64enc | quote }}
  {{- end }}
